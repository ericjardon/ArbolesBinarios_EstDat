public class impresoraDeArbol
{
    private Arbol a;

    public impresoraDeArbol(Arbol a){
        this.a = a;
    }
        // debe ser una impresión igualmente recursiva? el problema es la naturaleza línea por línea con la que imprime.
        // debe imprimir por niveles, del menor al más profundo.
        // debe tomar en cuenta el ancho y el número de tabulaciones que debe de hacer.
        // debe tomar en cuenta las líneas que se usan para aristas y las que se usan para nodos.

}
